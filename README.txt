# Project Structure
###The directory structure of the project looks like this:

├── LatexRepports                          <- Latex report and latex packages
├── Other (un)useful stuff                 <- Other documents
├── andras_analysis                        <- Andras's stuff
├── cycy_analysis                          <- Cyrielle's stuff
├── data                                   <- Cleaned data files
├── newyorker_caption_contest_virgin       <- Virgin data
│
├── src                                    <- Source code
│   ├── data                                   <- Data directory
│   ├── models                                 <- Model directory
│   ├── utils                                         <- Utility directory
│   ├── scripts                                       <- Shell scripts
│
├── tests                                        <- Tests of any kind
│
├── results.ipynb                                <- a well-structured notebook showing the results *(TO CREATE)*
│
├── .gitignore                                   <- List of files ignored by git
├── pip_requirements.txt                         <- File for installing python dependencies  *(TO CREATE)*
└── README.md
